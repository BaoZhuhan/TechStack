#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>
#include <string.h>

typedef struct node {

}list* ;


list initlist(){
    list l;
    l = (list )malloc(sizeof(struct node));
    return l;
}

void clearlist(list l ){

}

void destorylist(){

}

void sortedinsert(){
    // //cp -> the first node > the given node
    // //ap is the front of cp
    // //NULL->ap->cp
    // if(ap == NULL){
    //     p->next = cp;
    //     H = p; // ? 

    // }else{
    //     p->next = cp;
    //     cp->next = p;
    // }

    //有序插入
    

}
